package practice2;

public class AbsoluteNumberCalculator {
    public int findAbsolute(int number) {
        if (number < 0) {
            return -number;
        } else
            return number;
    }
}
